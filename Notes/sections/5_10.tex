The overall picture of our learning algorithm $A$ should be as follows. \vspace{3.5pt}
\begin{center}
    % \includegraphics{}
\end{center} \vspace{3.5pt}
Now the pipeline has two new elements, which are:
\begin{enumerate}
    \item \textbf{Confidence} $\delta$, error induced by the learning algorithm when it returns the classifier $C$.
    \item \textbf{Accuracy} $\epsilon$, error produced not anymore over the training data but after the usage of the testing data.
\end{enumerate}